# XinYu Dataset Library

Future external/public dataset samples should live here.

Current code resolves this directory first, then falls back to the legacy live
path under `XinYu-Core/examples/agent-apps/xinyu/data/external/`. Raw dataset
files remain local material and must not be promoted into lived memory.
