[pytest]
addopts = --import-mode=importlib -m "not smoke"
pythonpath =
    .
    tests
testpaths = tests
asyncio_mode = auto
markers =
    smoke: standalone integration smoke scripts (run with `-m smoke`; excluded by default)
norecursedirs =
    .git
    .venv
    data
    learning
    runtime
