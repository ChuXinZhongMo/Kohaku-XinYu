# Archived Custom Manifests

Archived on 2026-05-17 during the subtractive refactor.

These files were thin constant-only manifests for older maintenance and memory pipelines. Repo-local search found no live runtime, config, startup, or test imports after the maintenance bridge collapse. They are kept here for provenance instead of being deleted outright.

Use the active bridge plugins and shared helpers in `custom/maintenance_bridge_utils.py` as the source of truth for current maintenance pipeline paths.
