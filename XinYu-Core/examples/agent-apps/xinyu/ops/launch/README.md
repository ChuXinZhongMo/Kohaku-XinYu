# Launch

Operator launch helpers live here. They may load local environment files or
start interactive runtime processes, but they are not imported by the live turn
path.

- `run_local_xinyu.py` runs XinYu from the local framework source without a package install. The app-root `run_local_xinyu.py` is only a compatibility wrapper.
